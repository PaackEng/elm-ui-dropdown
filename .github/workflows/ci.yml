name: CI
#on:
#  pull_request:
#    types: [opened, synchronize, reopened, ready_for_review]
#  push:
#    branches: [master]
#    tags:
#      - '*'

jobs:
  docs:
   runs-on: ubuntu-latest
#   if: github.event.pull_request.draft == false

   steps:
     - name: Checkout
       uses: actions/checkout@v2

     - name: Validate docs
       run: npx elm make --docs=/tmp/docs.json

  example1:
   runs-on: ubuntu-latest
#   if: github.event.pull_request.draft == false

   steps:
     - name: Checkout
       uses: actions/checkout@v2

     - name: Validate code
       run: elm make examples/Example1.elm --output /dev/null
